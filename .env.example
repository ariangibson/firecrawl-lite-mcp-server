# Your LLM API key (xAI, OpenAI, Anthropic, etc.)
LLM_API_KEY=your_llm_api_key_here

# LLM provider (xai, openai, anthropic)
LLM_PROVIDER=xai

# LLM model to use
LLM_MODEL=grok-code-fast-1

# Optional: Firecrawl API URL (leave empty for cloud)
FIRECRAWL_API_URL=

# Optional: Firecrawl API key (if using cloud service)
FIRECRAWL_API_KEY=
